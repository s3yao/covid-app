import React from 'react';
import { Text, StyleSheet } from 'react-native';

const LoginScreen = () => {
    return <Text style={styles.text}>Login Screen</Text>
};

const styles = StyleSheet.create({
    text: {
        color: 'green'
    }
});

export default LoginScreen;